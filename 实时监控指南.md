# 飞书状态分析 - 实时监控脚本

## 第一步：清空日志并开始监控

现在在你的PowerShell中运行这个命令开始监控：

```powershell
adb logcat | Select-String -Pattern "lark.*status|lark.*Status|lark.*custom|lark.*Custom|lark.*presence|lark.*Presence"
```

## 第二步：操作飞书

在手机上：
1. 打开飞书应用
2. 点击右下角"我" 
3. 点击头像或用户名
4. 找到"设置状态"或"自定义状态"
5. 修改状态文本（例如：摸鱼中🐟）
6. 点击保存

## 第三步：观察输出

监控窗口会显示飞书操作时的日志，包含：
- 类名（如：CustomStatusActivity, StatusManager）
- 方法名（如：setStatus, updateCustomStatus）
- API调用（如：/api/custom_status）

## 备用方案：如果日志太多

使用更精确的过滤：

```powershell
adb logcat Lark:V *:S | Select-String "status|Status|custom|Custom"
```

## 获取类结构

如果看到类名后，运行：

```powershell
adb shell "dumpsys package com.ss.android.lark" | Select-String -Pattern "类名"
```

例如：
```powershell
adb shell "dumpsys package com.ss.android.lark" | Select-String -Pattern "StatusManager"
```

---

**准备好了告诉我，我会等你的输出结果！**
