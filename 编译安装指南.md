# 飞书自动状态切换插件 - 编译和安装指南

## 📋 文件清单

- `Tweak.x` - 主要代码（基于Android逆向分析优化）
- `Makefile` - 编译配置
- `control` - deb包信息
- `FeishuAutoStatus.plist` - 注入配置

## 🛠️ 编译步骤

### 方法1：在macOS上编译（推荐）

#### 前置条件
1. 安装Xcode Command Line Tools
2. 安装Theos
   ```bash
   bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
   ```

#### 编译
```bash
cd /path/to/FeishuAutoStatus
make clean
make package
```

生成的deb文件在 `packages/` 目录。

### 方法2：使用Docker（跨平台）

```bash
docker run --rm -v "${PWD}:/workspace" -w /workspace theos/theos make package
```

### 方法3：使用GitHub Actions（自动化）

创建 `.github/workflows/build.yml` 文件，提交到仓库后自动编译。

## 📦 手动打包（如果没有Theos）

1. 创建目录结构：
```
com.yourname.feishuautostatus_1.0.0_iphoneos-arm64/
├── DEBIAN/
│   └── control
└── Library/
    └── MobileSubstrate/
        └── DynamicLibraries/
            ├── FeishuAutoStatus.dylib
            └── FeishuAutoStatus.plist
```

2. 使用dpkg打包：
```bash
dpkg-deb -Zgzip -b com.yourname.feishuautostatus_1.0.0_iphoneos-arm64
```

## 📲 安装方法

### 方法1：Filza安装（最简单）
1. 将.deb文件通过爱思助手传到手机的Documents目录
2. 用Filza打开Documents目录
3. 点击.deb文件
4. 点击"安装"
5. 重启SpringBoard或重新打开飞书

### 方法2：命令行安装
```bash
# 通过SSH连接到iPhone
ssh root@<iPhone-IP>
# 默认密码：alpine

# 安装
dpkg -i /path/to/package.deb

# 重启SpringBoard
killall -9 SpringBoard
```

### 方法3：Cydia/Sileo安装
1. 将.deb上传到自己的源
2. 在Cydia/Sileo中添加源并安装

## 🔧 配置文件

配置文件位置：`/var/mobile/Library/Preferences/com.yourname.feishuautostatus.plist`

示例配置：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>enabled</key>
    <true/>
    <key>interval</key>
    <integer>3600</integer>
    <key>workTimeEnabled</key>
    <true/>
    <key>workStartHour</key>
    <integer>8</integer>
    <key>workStartMinute</key>
    <integer>0</integer>
    <key>workEndHour</key>
    <integer>17</integer>
    <key>workEndMinute</key>
    <integer>30</integer>
    <key>offWorkStatus</key>
    <string>休息中 🌙</string>
    <key>statusList</key>
    <array>
        <dict>
            <key>text</key>
            <string>在线</string>
            <key>emoji</key>
            <string>✅</string>
        </dict>
        <dict>
            <key>text</key>
            <string>忙碌</string>
            <key>emoji</key>
            <string>🔥</string>
        </dict>
        <dict>
            <key>text</key>
            <string>会议中</string>
            <key>emoji</key>
            <string>📞</string>
        </dict>
    </array>
</dict>
</plist>
```

## 🐛 调试

### 查看日志
```bash
# 实时查看日志
ssh root@<iPhone-IP>
log stream --predicate 'processImagePath contains "Lark"' --level debug

# 或者使用grep过滤
log stream | grep FeishuAutoStatus
```

### 常见问题

1. **插件不生效**
   - 检查是否安装了Substrate
   - 确认飞书Bundle ID是否正确
   - 重启SpringBoard

2. **找不到状态管理类**
   - 查看日志中的类扫描结果
   - 飞书版本可能有变化，需要重新分析

3. **状态没有切换**
   - 检查配置文件是否正确
   - 确认定时器是否启动
   - 查看是否在工作时间内

## 📝 技术说明

### 基于Android逆向分析

通过ADB和logcat分析Android版飞书，发现关键类：
```
com.ss.android.lark.mine.impl.custom_status.UserCustomStatusActivity
```

iOS对应类名推测：
- `LKCustomStatusManager`
- `LKUserCustomStatusManager`
- `CustomStatusManager`

### 智能适配机制

插件使用运行时扫描技术：
1. 尝试已知的类名
2. 扫描所有包含"Status"和"Manager"的类
3. 动态调用多种可能的方法签名
4. 监控所有ViewController来捕获状态界面

### 日志输出说明

- 🚀 应用启动
- 📦 Bundle信息
- 🔍 类扫描
- ✅ 找到目标类
- 📺 界面出现
- ⏰ 定时器启动
- 📢 状态通知
- ❌ 错误信息

## 🔄 更新插件

如果飞书更新导致插件失效：

1. 重新运行Android分析（如果可用）
2. 查看日志中扫描到的类名
3. 更新 `Tweak.x` 中的 `possibleClasses` 数组
4. 重新编译安装

## 📞 技术支持

遇到问题请提供：
1. 飞书版本号
2. iOS版本
3. 完整的日志输出
4. 配置文件内容
