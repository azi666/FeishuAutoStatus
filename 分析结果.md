# 🎯 飞书状态管理分析结果

## ✅ 已发现的关键信息

### Android 类名结构

从日志分析得到：

```
包路径：com.ss.android.lark.mine.impl.custom_status
Activity：UserCustomStatusActivity
```

### 可能的类名推断

根据Android包结构，可能的相关类：

1. **Activity**（界面）
   - `UserCustomStatusActivity` ✅ 已确认

2. **Manager**（管理器）
   - `CustomStatusManager`
   - `UserCustomStatusManager`

3. **Model**（数据模型）
   - `CustomStatus`
   - `UserCustomStatus`
   - `CustomStatusModel`

4. **Service/API**（服务层）
   - `CustomStatusService`
   - `CustomStatusApi`

### iOS 对应关系

飞书iOS和Android通常使用相同的命名规范，类名映射：

| Android | iOS (Objective-C) | 说明 |
|---------|-------------------|------|
| `com.ss.android.lark.mine.impl.custom_status.UserCustomStatusActivity` | `LKUserCustomStatusViewController` 或 `UserCustomStatusViewController` | 界面控制器 |
| `CustomStatusManager` | `LKCustomStatusManager` 或 `CustomStatusManager` | 状态管理器 |
| `UserCustomStatus` | `LKUserCustomStatus` 或 `UserCustomStatus` | 数据模型 |

### 关键方法推测

根据Android常见模式，iOS可能有这些方法：

```objective-c
// 设置状态
- (void)setCustomStatus:(NSString *)text emoji:(NSString *)emoji;
- (void)updateCustomStatus:(LKUserCustomStatus *)status;

// 获取状态
- (LKUserCustomStatus *)getCurrentCustomStatus;

// 清除状态
- (void)clearCustomStatus;
```

## 🔍 下一步分析

需要进一步确认的信息：

1. **Manager类的完整类名**
2. **设置状态时调用的具体方法**
3. **数据模型的字段结构**（text, emoji, expireTime等）

### 方法1：抓取网络请求

查看API调用可以了解数据结构：

```powershell
adb shell "tcpdump -i any -s 0 -w /sdcard/feishu.pcap host open.feishu.cn"
```

### 方法2：使用Frida动态Hook

由于已经有了包路径，可以直接Hook这个Activity。

### 方法3：反编译APK

使用jadx反编译飞书APK，直接查看源码。

## 💡 当前最佳策略

基于已有信息，我建议：

**先编写通用版iOS插件**，使用以下策略：

1. **Hook所有可能的类名**
   ```objective-c
   %hook LKCustomStatusManager
   %hook CustomStatusManager
   %hook LKUserCustomStatusManager
   ```

2. **Hook所有包含"status"的方法**
   使用通配符Hook技术

3. **运行时探测**
   插件启动时扫描所有已加载的类，找到真实的类名

这样即使类名猜错了，插件也能自动适配！

---

## 📝 记录

- **Android包名**：`com.ss.android.lark`
- **状态模块路径**：`mine.impl.custom_status`
- **Activity**：`UserCustomStatusActivity` ✅
- **分析时间**：2026-08-12
- **飞书版本**：未知（建议记录）
