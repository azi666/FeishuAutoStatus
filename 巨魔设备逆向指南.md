# 飞书状态管理类逆向分析指南（巨魔设备）

## 📱 准备工作

### 1. 在巨魔设备上安装工具

**方法A：使用Frida（推荐）**
```bash
# 在电脑上安装frida-tools
pip install frida-tools

# 在iOS设备上安装Frida（通过Sileo/Zebra）
# 或者下载Frida.deb手动安装
```

**方法B：使用class-dump（静态分析）**
- 下载Flexdecrypt或bagbak砸壳飞书IPA
- 使用class-dump导出头文件

### 2. 获取飞书Bundle ID

打开飞书，在设置中查看，通常是：
- `com.ss.iphone.lark`（国内版）
- `com.larksuite.suite`（国际版）

---

## 🔍 方案一：Frida动态分析（最快速）

### 步骤1：连接设备

```bash
# USB连接设备后，在电脑上执行
frida-ps -U

# 应该能看到设备上运行的进程列表
```

### 步骤2：运行Frida脚本

```bash
cd "D:\Users\2403050002\Desktop\py\优化\FeishuAutoStatus"

# 启动飞书并注入脚本
frida -U -f com.ss.iphone.lark -l frida_hook_status.js --no-pause

# 或者附加到已运行的飞书
frida -U Lark -l frida_hook_status.js
```

### 步骤3：触发状态变化

在手机上操作：
1. 打开飞书
2. 点击个人头像
3. 点击"设置状态"或"我的状态"
4. 尝试切换不同的状态（在线、忙碌、休息等）

### 步骤4：查看输出

Frida会打印出：
- 所有包含"Status"的类名
- 调用的方法名
- 方法参数
- 通知名称

**重点关注：**
```
[Hook] XXXStatusManager.setStatus:
[Hook] XXXStatusManager.updateUserStatus:
[通知] UserStatusDidChangeNotification
```

把输出的**完整日志**发给我！

---

## 🔍 方案二：使用Flexdecrypt砸壳（更详细）

### 步骤1：安装Flexdecrypt

```bash
# 在巨魔设备上安装Flexdecrypt（TrollStore可直接安装）
# 下载地址：https://github.com/JohnCoates/flexdecrypt
```

### 步骤2：砸壳飞书

打开Flexdecrypt -> 选择飞书 -> Decrypt

会在Documents目录生成解密的IPA

### 步骤3：导出IPA到电脑

```bash
# 使用爱思助手/iTunes/iMazing导出IPA
# 或使用SSH/文件管理器拷贝
```

### 步骤4：解压IPA并使用class-dump

```bash
# 解压IPA
unzip Lark.ipa

# 进入Payload目录
cd Payload/Lark.app

# 使用class-dump（需要在Mac上）
class-dump Lark > Lark_headers.h

# 搜索状态相关类
grep -i "status" Lark_headers.h > status_classes.txt
```

把 `status_classes.txt` 发给我！

---

## 🔍 方案三：使用Hopper/IDA分析（最专业）

如果你会用Hopper或IDA：

1. 用Flexdecrypt砸壳导出IPA
2. 解压IPA获取主二进制文件
3. 用Hopper Disassembler打开
4. 搜索字符串："status"、"在线"、"忙碌"、"休息"
5. 查看引用这些字符串的类和方法

---

## 🔍 方案四：运行时查看（最简单）

### 使用FLEXible（推荐给新手）

1. 安装FLEXible（TrollStore或Sileo）
2. 打开飞书，激活FLEX（通常是状态栏手势）
3. 点击 Views -> 找到状态相关的View
4. 查看View的类名和属性
5. 点击 "探索对象"查看调用链

### 或使用Lookin

1. 安装Lookin（Mac端和iOS端）
2. 连接设备查看飞书的视图层级
3. 找到状态显示的View
4. 查看其类名和关联的ViewController

---

## 📋 你需要提供给我的信息

运行完上述任何方案后，提供以下信息：

### 关键信息清单：

```
1. 状态管理类名
   示例：LKStatusManager / LKUserStatusManager / XXStatusController

2. 设置状态的方法名
   示例：-[LKStatusManager setUserStatus:]
         -[LKStatusManager updateStatus:withText:]

3. 状态的数据结构
   - 是NSString？NSNumber？还是自定义对象？
   - 在线/忙碌/休息对应的值是什么？

4. 获取当前状态的方法
   示例：-[LKStatusManager currentUserStatus]

5. 相关的通知名称（如果有）
   示例：LKUserStatusDidChangeNotification
```

---

## 💡 快速测试方法

如果你只是想快速验证，可以：

1. 安装我们的插件（即使类名错误也能安装）
2. 打开飞书
3. 使用Filza或SSH查看日志：
   ```bash
   # 查看系统日志中的崩溃信息
   cat /var/mobile/Library/Logs/CrashReporter/Latest/*.ips
   
   # 或使用Console.app（Mac）连接设备查看实时日志
   ```
4. 崩溃日志会显示真实的类名

---

## 🚀 推荐流程（最快30分钟）

**最简单的方式：**

1. 安装Frida（10分钟）
2. 运行 `frida_hook_status.js` 脚本（2分钟）
3. 在飞书中切换状态（1分钟）
4. 拷贝完整日志发给我（1分钟）
5. 我更新代码并推送到GitHub（5分钟）
6. GitHub Actions自动构建新版本（10分钟）
7. 下载安装测试（5分钟）

总共：约30-40分钟

---

## ❓ 遇到问题？

**Frida无法连接：**
```bash
# 确保设备和电脑在同一网络
# 在设备上运行：
frida-server &

# 在电脑上测试连接：
frida-ps -U
```

**找不到类：**
- 飞书可能做了混淆或动态加载
- 尝试在不同页面触发（首页、聊天、设置页面）
- 使用Frida扫描所有模块：`Process.enumerateModules()`

**需要更多帮助：**
把你看到的任何输出、错误信息、截图发给我！
