# 巨魔设备逆向飞书方案（非越狱）

## ⚠️ 重要说明

**巨魔（TrollStore）≠ 越狱**

- ✅ 巨魔：可以安装未签名的IPA，但**无法注入插件**
- ✅ 越狱：可以注入Tweak插件修改系统和应用行为
- ❌ 我们创建的 `.deb` 插件**需要越狱环境才能使用**

---

## 🎯 针对巨魔设备的3种解决方案

### 方案1：使用越狱模拟器逆向（推荐）

在越狱模拟器或朋友的越狱设备上分析飞书，获取类名后，你再在真机测试。

**步骤：**
1. 使用Checkra1n等工具越狱一台测试设备（或虚拟机）
2. 安装Frida分析飞书
3. 获取真实类名和方法
4. 更新插件代码
5. 在你的巨魔设备上测试（如果可以临时越狱）

---

### 方案2：静态分析飞书IPA（可行）

不需要运行时分析，直接分析IPA文件。

#### 需要的工具：
- **class-dump**（提取Objective-C头文件）
- **Hopper Disassembler**（反汇编工具）
- **IDA Pro**（专业反汇编）
- 或者我可以帮你分析

#### 步骤：

1. **获取飞书IPA**
   
   **方式A：从设备导出**
   ```
   使用爱思助手/iMazing：
   - 应用管理
   - 找到飞书
   - 导出IPA
   ```
   
   **方式B：从iTunes备份提取**
   
   **方式C：从第三方网站下载**
   - https://www.ipa-apps.me/
   - https://appdb.to/

2. **解密IPA（如果需要）**
   
   App Store下载的IPA是加密的，需要解密：
   - 使用 **Flexdecrypt**（需要越狱设备）
   - 或使用 **bagbak**（Python工具）
   - 或我可以帮你找解密版

3. **分析头文件**
   
   ```bash
   # 解压IPA
   unzip Feishu.ipa
   
   # 使用class-dump
   class-dump Payload/Feishu.app/Feishu > headers.txt
   
   # 搜索状态相关类
   grep -i "status" headers.txt
   grep -i "在线" headers.txt
   grep -i "忙碌" headers.txt
   ```

4. **找到关键方法**
   
   查找包含以下关键字的类和方法：
   - `Status`
   - `UserStatus`
   - `setStatus`
   - `updateStatus`
   - `changeStatus`

5. **把结果发给我**

---

### 方案3：修改飞书IPA直接注入代码（终极方案）

既然你有巨魔，可以直接修改飞书IPA注入我们的代码！

#### 原理：
不安装系统级插件，而是：
1. 解包飞书IPA
2. 注入我们的dylib库
3. 修改可执行文件加载我们的库
4. 重新打包签名
5. 用巨魔安装修改后的IPA

#### 需要的工具：
- **insert_dylib**（注入dylib）
- **optool**（修改MachO）
- **ldid**或**zsign**（重签名）
- **Theos**（编译dylib）

#### 步骤：

1. **编译成dylib而不是Tweak**
   ```bash
   # 修改Makefile
   TARGET = iphone:clang:latest:14.0
   ARCHS = arm64
   
   include $(THEOS)/makefiles/common.mk
   
   LIBRARY_NAME = FeishuAutoStatus
   FeishuAutoStatus_FILES = Tweak.x
   FeishuAutoStatus_FRAMEWORKS = UIKit Foundation
   
   include $(THEOS)/makefiles/library.mk
   ```

2. **编译dylib**
   ```bash
   make
   # 得到 FeishuAutoStatus.dylib
   ```

3. **注入到飞书IPA**
   ```bash
   # 解压IPA
   unzip Feishu.ipa
   
   # 注入dylib
   insert_dylib @executable_path/Frameworks/FeishuAutoStatus.dylib \
       Payload/Feishu.app/Feishu \
       --all-yes --inplace
   
   # 拷贝dylib
   cp FeishuAutoStatus.dylib Payload/Feishu.app/Frameworks/
   
   # 重签名
   ldid -S Payload/Feishu.app/Feishu
   ldid -S Payload/Feishu.app/Frameworks/FeishuAutoStatus.dylib
   
   # 重新打包
   zip -r Feishu_Modified.ipa Payload
   ```

4. **用巨魔安装**
   ```
   AirDrop发送到iPhone → 用巨魔打开安装
   ```

**优点**：
- ✅ 不需要越狱
- ✅ 直接在你的设备运行
- ✅ 功能完整

**缺点**：
- ❌ 需要编译环境（Mac或Linux）
- ❌ 步骤复杂
- ❌ 每次飞书更新都要重新注入

---

## 🎯 我的建议

### 最快方案：静态分析（方案2）

1. **你把飞书IPA发给我**
   - 从设备导出
   - 或者告诉我飞书版本，我去找

2. **我帮你分析**
   - 提取头文件
   - 找到状态管理类
   - 找到相关方法

3. **更新代码**
   - 我更新Tweak.x
   - 推送到GitHub

4. **你临时越狱测试**
   - 使用palera1n/Dopamine临时越狱
   - 安装插件测试
   - 确认可用

5. **或者我帮你打包修改版IPA（方案3）**

---

## 🔄 现在需要你做什么？

### 选项A：提供飞书IPA
```
使用爱思助手导出飞书IPA，发给我分析
```

### 选项B：告诉我飞书版本
```
我去下载对应版本分析
设置 → 通用 → 关于 → 版本号
```

### 选项C：临时越狱
```
iOS 16.4.1 可以使用 Dopamine 越狱
安装后运行Frida分析
```

---

## 💡 补充：palera1n/Dopamine越狱

你的iPhone 14 Pro Max iOS 16.4.1可以使用：

**Dopamine越狱**
- 下载：https://ellekit.space/dopamine/
- 支持iOS 15.0 - 16.6.1
- 支持A12-A16设备
- 可随时还原

安装后就能用我们的插件了！

---

## 📞 你现在选择哪个方案？

告诉我：
- **A** - 我导出飞书IPA给你分析
- **B** - 我告诉你版本号，你去下载
- **C** - 我考虑安装Dopamine越狱

我会根据你的选择继续！
