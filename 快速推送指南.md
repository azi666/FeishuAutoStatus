# 🎯 快速推送指南

## ⚡ 最快方式：运行批处理脚本

双击运行：`一键推送到GitHub.bat`

按提示操作即可！

---

## 📝 手动推送步骤

### 1️⃣ 创建GitHub仓库

访问：https://github.com/new

- 仓库名：`FeishuAutoStatus`
- 可见性：Private（推荐）
- ❌ 不要勾选任何初始化选项

### 2️⃣ 在PowerShell执行

```powershell
cd D:\Users\2403050002\Desktop\py\优化\FeishuAutoStatus

# 添加远程仓库（替换成你的地址）
git remote add origin https://github.com/你的用户名/FeishuAutoStatus.git

# 推送
git push -u origin main
```

### 3️⃣ 如果提示认证失败

**获取Personal Access Token：**

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成并复制token（以`ghp_`开头）

**重新推送时输入：**
```
Username: 你的GitHub用户名
Password: ghp_xxxxxxxxxxxx (token，不是密码！)
```

### 4️⃣ 查看自动编译

推送成功后：

1. 访问：`https://github.com/你的用户名/FeishuAutoStatus/actions`
2. 点击最新的workflow
3. 等待编译完成（绿色✓）
4. 下载 "Artifacts" 中的 `FeishuAutoStatus-deb`

---

## 🐛 常见问题

**Q: git命令找不到？**

安装Git：
```powershell
winget install Git.Git
```

**Q: 推送被拒绝？**

```powershell
# 强制推送（如果确定要覆盖）
git push -u origin main --force
```

**Q: GitHub Actions没有运行？**

检查：
1. `.github/workflows/build.yml` 文件是否存在
2. 仓库Settings -> Actions -> 确认Actions已启用

---

## 📞 需要帮助？

把错误信息告诉我，我会立即协助解决！
